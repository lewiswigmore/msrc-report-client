# =============================================================================
# MSRC Abuse Reporting Portal - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local or any file containing real secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# Azure AD / Microsoft Entra ID Configuration (Required)
# -----------------------------------------------------------------------------
# Create an App Registration in Azure AD:
# 1. Go to Azure Portal > Microsoft Entra ID > App registrations
# 2. Click "New registration"
# 3. Set a name, select "Single tenant" for internal-only access
# 4. Set redirect URI to your app URL (e.g., http://localhost:3000 for dev)
# 5. Copy the Application (client) ID and Directory (tenant) ID below
# -----------------------------------------------------------------------------

# Application (client) ID from Azure AD App Registration
NEXT_PUBLIC_AZURE_CLIENT_ID=your-application-client-id-here

# Directory (tenant) ID from Azure AD (your organization's tenant)
# Using your org's tenant ID restricts access to your organization only
NEXT_PUBLIC_AZURE_TENANT_ID=your-tenant-id-here

# Redirect URI for authentication (defaults to window.location.origin if not set)
# For local development, use: http://localhost:3000
# For production, use your deployed app URL
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000

# -----------------------------------------------------------------------------
# Optional: Custom API Scope
# -----------------------------------------------------------------------------
# If you've registered a custom API scope in Azure AD for the MSRC API,
# set it here. Otherwise, default scopes (openid, profile, email, User.Read)
# will be used for basic authentication.
# -----------------------------------------------------------------------------

# NEXT_PUBLIC_MSRC_API_SCOPE=api://your-api-scope/.default

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------

# Enable verbose MSAL authentication logging (true/false)
# Automatically enabled in development mode
NEXT_PUBLIC_ENABLE_AUTH_LOGGING=false
